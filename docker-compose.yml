version: "3.9"
services:
  mysql:
    image: mysql:8.0
    container_name: smart_monitoring_mysql
    environment:
      MYSQL_ROOT_PASSWORD: root
      MYSQL_DATABASE: smart_monitoring
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql

volumes:
  mysql_data:
